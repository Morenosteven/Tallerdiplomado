name: CI/CD Taller

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v4
      - name: Verificar existencia de archivos base
        run: |
          if [ ! -f home.html ]; then echo 'Error: Falta home.html'; exit 1; fi
          if [ ! -f styles.css ]; then echo 'Error: Falta styles.css'; exit 1; fi
          echo 'Archivos verificados correctamente ✅'

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Simular despliegue
        run: echo 'Artefactos listos para el despliegue en el entorno de Staging 🚀'
